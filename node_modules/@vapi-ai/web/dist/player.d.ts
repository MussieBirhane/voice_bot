/// <reference types="node" />
import EventEmitter from "events";
export declare class ContinuousPlayer extends EventEmitter {
    private atomic;
    private audio;
    constructor();
    start(): void;
    playChunk(audioData: ArrayBuffer): void;
    clear(): void;
}
